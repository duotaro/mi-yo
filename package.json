{
  "name": "nextjs",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "export": "next export",
    "deploy": "next build && next export && git add . && git commit -m 'release' && git push origin gh-pages"
  },
  "dependencies": {
    "@notionhq/client": "^2.2.3",
    "@types/firebase": "^3.2.1",
    "firebase": "^9.20.0",
    "firebase-functions": "^4.3.1",
    "next": "13.1.6",
    "react": "18.2.0",
    "react-dom": "18.2.0"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
